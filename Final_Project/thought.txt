针对静态、动态动作分别识别：
先由box的位置变化判定是否是动态运动
如果是静态运动：
	利用openpose处理后每一帧的输出信息训练分类网络（简单的NN）
如果是动态运动：
	考虑用RNN网络对pose序列进行判定->行走、跑步、摔倒（仅考虑这三类）
	考虑用算法对分析人体的关键帧，判定是否有疑似施暴行为或被害举动（如身体前倾且出拳、身体后仰等）

基于原作者获得一帧中所有人ID后，返回一个字典dict，字典名均为每个人的ID，字典值分别为所有的关节点坐标
for i in dict:
	比对i与上一次采样时的centerx的位移关系(如果有上一次采样的话)
	i是动态运动:
		是否**(如打人、行走、奔跑、摔倒)
	i是静态运动:
		是否**(如站立、坐着、打人)
#是否**(input=第i个人的姿态，上一次采样时此人的姿态（如果有的话)) ->返回判定
	
